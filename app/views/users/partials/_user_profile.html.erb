<span class="icon"><%= avatar_for user, link_it: false %></span>
<h1 class="name">
  <% if (link |= false) %>
    <%= link_to user.username, user_path(user.username) %>
  <% else %>
    <%= user.username %>
  <% end %>
</h1>